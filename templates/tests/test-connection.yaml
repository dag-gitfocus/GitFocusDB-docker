apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitfocus-postgres.fullname" . }}-test-connection"
  labels:
    {{- include "gitfocus-postgres.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitfocus-postgres.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
