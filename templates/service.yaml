apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitfocus-postgres.fullname" . }}
  labels:
    {{- include "gitfocus-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitfocus-postgres.selectorLabels" . | nindent 4 }}
